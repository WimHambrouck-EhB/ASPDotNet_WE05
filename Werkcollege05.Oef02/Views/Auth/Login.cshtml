@model IEnumerable<Werkcollege05.Oef02.Models.Gebruiker>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Wachtwoord)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Wachtwoord)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Naam">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Naam">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Naam">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
